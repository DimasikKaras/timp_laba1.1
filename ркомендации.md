1. Структура и роутинг SPA
   1. Создание основных страниц. В src удалим все, что создалось при сборке проекта кроме файлов (App.js index.js) и создадим папку pages, а в ней файлы:
      1. Home.js (главная страница)
      1. Detail.js (страница детализации)
      1. Form.js (страница формы)
   1. Настройте роутинг. Для того чтобы настроить роутинг, нам необходимо изменить содержания файла App.js.
1. Связь с сервером (RESTful API)
   1. Установка и настройка json-server

npm install -g json-server

1. Создайте файл db.json в корне проекта
1. Запустим сервер с помощью команды
1. Проверка работоспособности. С помощью утилиты Postman проверим, что сервер действительно работает, отправим GET запрос.

Можем убедиться, что сервер корректно работает.

1. Реализуйте в SPA следующие операции над выбранной сущностью через HTTP-запросы:
   1. GET – получение списка сущностей (вывод на главной странице).

В компоненте Home.js напишите код для отправки GET запроса.

import React from 'react';

import { Link } from 'react-router-dom';

import axios from 'axios';

let data = [];

async function loadData() {

`  `try {

`    `const response = await axios.get("http://localhost:5000/items");

`    `data = response.data;

`    `console.log("Данные загружены:", data);

`  `} catch (error) {

`    `console.error("Ошибка запроса:", error);

`  `}

}

await loadData();

const Home = () => {



`    `return (

`        `<div>

`            `<h1>Список товаров</h1>

`            `<ul>

`                `{data.map(item => (

`                    `<li key={item.id}>

`                        `<Link to={`/detail/${item.id}`}>{item.name}</Link>

`                    `</li>

`                `))}

`            `</ul>

`            `<Link to="/add">Добавить товар</Link>

`        `</div>

`    `);

};

export default Home;

1. POST – добавление новой сущности (например, по нажатию кнопки «Сохранить» на странице формы).

В компоненте Form.js напишите код для отправки POST запроса.

1. import React from 'react';
1. import axios from 'axios';
1. import { useNavigate } from 'react-router-dom';

1. const Form = () => {
1. `    `let name = ''; // Используем обычную переменную
1. `    `const navigate = useNavigate();

1. `    `const handleChange = (e) => {
1. `        `name = e.target.value;
1. `        `console.log("Текущее значение name:", name);
1. `    `};

1. `    `const handleSubmit = (e) => {
1. `        `e.preventDefault();
1. `        `axios.post('http://localhost:5000/items', { name })
1.             .then(() => navigate('/'))
1.             .catch(error => console.error(error));
1. `    `};

1. `    `return (
1. `        `<form onSubmit={handleSubmit}>
1. `            `<input type="text" onChange={handleChange} required />
1. `            `<button type="submit">Сохранить</button>
1. `        `</form>
1. `    `);
1. };

1. export default Form;

3\.2.3.PUT (или PATCH) – обновление существующей сущности.

В компоненте Detail.js напишите код для отправки PUT запроса

1. import React, { useRef } from 'react';
1. import { useParams, useNavigate } from 'react-router-dom';
1. import axios from 'axios';

1. const Detail = () => {
1. `    `const { id } = useParams();
1. `    `const navigate = useNavigate();

1. `    `let item = {}; // Обычная переменная для хранения данных
1. `    `const nameRef = useRef(null);

1. `    `// Загружаем данные перед рендером (нежелательно в React, но можно)
1. `    `axios.get(`http://localhost:5000/items/${id}`)
1.         .then(response => {
1. `            `item = response.data;
1. `            `if (nameRef.current) {
1. `                `nameRef.current.value = item.name;
       
1. `            `}
1. `        `})
1.         .catch(error => console.error("Ошибка загрузки:", error));

1. `    `// Функция для сохранения изменений
1. `    `const handleSubmit = (e) => {
1. `        `e.preventDefault();
1. `        `const updatedData = {
1. `            `name: nameRef.current.value,
1. `        `};

1. `        `axios.put(`http://localhost:5000/items/${id}`, updatedData)
1.             .then(() => {
1. `                `alert("Данные обновлены!");
1. `                `navigate('/');
1. `            `})
1.             .catch(error => console.error("Ошибка обновления:", error));
1. `    `};

1. `    `return (
1. `        `<div>
1. `            `<h1>Редактирование</h1>
1. `            `<form onSubmit={handleSubmit}>
1. `                `<label>
1. `                    `Название:
1. `                    `<input type="text" ref={nameRef} required />
1. `                `</label>
1. `                `<br />
1. `                `<button type="submit">Сохранить</button>
1. `            `</form>
1. `        `</div>
1. `    `);
1. };

1. export default Detail;


3\.2.4.	DELETE – удаление сущности (например, кнопка «Удалить» в списке).

Добавить в компонент Home.js код для отправки DELET запроса.

import React from 'react';

import { Link } from 'react-router-dom';

import axios from 'axios';

let data = []; // Обычная переменная для хранения данных

// Функция для загрузки данных

async function loadData() {

`  `try {

`    `const response = await axios.get("http://localhost:5000/items");

`    `data = response.data;

`    `console.log("Данные загружены:", data);

`  `} catch (error) {

`    `console.error("Ошибка запроса:", error);

`  `}

}

// Загружаем данные перед рендером

await loadData();

// Функция для удаления товара

function deleteItem(id) {

`  `axios.delete(`http://localhost:5000/items/${id}`)

.then(() => {

`      `console.log(`Товар ${id} удален`);

`      `// Удаляем товар из списка вручную

`      `data = data.filter(item => item.id !== id);

`      `console.log("Обновленные данные:", data);

`    `})

.catch(error => console.error("Ошибка удаления:", error));

}

const Home = () => {

`  `return (

`    `<div>

`      `<h1>Список товаров</h1>

`      `<ul>

`        `{data.map(item => (

`          `<li key={item.id}>

`            `<Link to={`/detail/${item.id}`}>{item.name}</Link>

`            `<button onClick={() => deleteItem(item.id)} style={{ marginLeft: "10px" }}>

`              `Удалить

`            `</button>

`          `</li>

`        `))}

`      `</ul>

`      `<Link to="/add">Добавить товар</Link>

`    `</div>

`  `);

};

export default Home;


1. Работа с JSON-данными
   1. Получайте JSON-ответ от сервера и преобразуйте его в объекты/массивы JavaScript (через fetch, axios или встроенные средства фреймворка).

Посмотрите, как реализован запрос в компонент Home.js, он с самого начала преобразуется json файл в массив.

1. При создании/обновлении сущности отправляйте JSON в теле запроса (методы POST/PUT).

Изменим компоненты Detail.js и Form.js так, чтобы в запрос они передавали не объект а json.

Detail.js

import React, { useRef } from 'react';

import { useParams, useNavigate } from 'react-router-dom';

import axios from 'axios';

const Detail = async () => {

`    `const { id } = useParams();

`    `const navigate = useNavigate();

`    `let itemData = {}; // Локальная переменная для хранения данных товара

`    `async function loadItem() {

`        `try {

`            `const response = await axios.get(`http://localhost:5000/items/${id}`);

`            `itemData = response.data;

`            `console.log("Загруженный товар:", itemData);

`        `} catch (error) {

`            `console.error("Ошибка загрузки:", error);

`        `}

`    `}

`    `await loadItem();

`    `const nameRef = useRef(null);

`    `const descriptionRef = useRef(null);

`    `if (nameRef.current && descriptionRef.current) {

`        `nameRef.current.value = itemData.name || '';

`    `}

`    `// Функция обновления товара

`    `const handleSubmit = (e) => {

`        `e.preventDefault();

`        `const updatedItem = {

`            `name: nameRef.current.value,

`        `};

`        `axios.put(`http://localhost:5000/items/${id}`, JSON.stringify(updatedItem), {

`            `headers: { "Content-Type": "application/json" }

`        `})

.then(response => {

`                `itemData = response.data;

`                `console.log("Обновленный товар:", itemData);

`                `navigate('/');

`            `})

.catch(error => console.error("Ошибка обновления:", error));

`    `};

`    `return (

`        `<div>

`            `<h1>Редактирование товара</h1>

`            `<form onSubmit={handleSubmit}>

`                `<label>

`                    `Название:

`                    `<input type="text" ref={nameRef} required />

`                `</label>

`                `<br />

`                `<button type="submit">Сохранить</button>

`            `</form>

`        `</div>

`    `);

};

export default Detail;


Form.js

import React, { useRef } from 'react';

import { useNavigate } from 'react-router-dom';

import axios from 'axios';

const Form = () => {

`    `const nameRef = useRef(null);

`    `const navigate = useNavigate();

`    `let newItemData = {}; // Локальная переменная для нового товара

`    `// Функция добавления товара

`    `const handleSubmit = (e) => {

`        `e.preventDefault();

`        `newItemData = {

`            `name: nameRef.current.value,

`        `};

`        `axios.post("http://localhost:5000/items", JSON.stringify(newItemData), {

`            `headers: { "Content-Type": "application/json" }

`        `})

.then(response => {

`                `console.log("Добавленный товар:", response.data);

`                `navigate('/');

`            `})

.catch(error => console.error("Ошибка создания:", error));

`    `};

`    `return (

`        `<form onSubmit={handleSubmit}>

`            `<label>

`                `Название:

`                `<input type="text" ref={nameRef} required />

`            `</label>

`            `<br />

`            `<button type="submit">Добавить</button>

`        `</form>

`    `);

};

export default Form;



1. 3Отображайте полученные данные на интерфейсе (список, таблица, карточки – на ваш выбор).

Добавьте в db.json новые поля, для объектов и отобразите их.
